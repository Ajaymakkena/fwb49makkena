extends layout

block content
  h1= title
   - var image_names=["ant1.jpg","ant2.jpg","ant3.jpg","ant4.jpg","ant5.jpg"]
     - for(let i=0; i<3; i++) {
        - var s= Math.floor(Math.random()*5)
           img(src=`/images/${image_names[s]}` alt=`${s}` width='300px' height='300px')
     - }